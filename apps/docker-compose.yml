services:
  kilsiworld-backend:
    image: kilsiworld-backend
    container_name: kilsiworld-backend
    ports:
      - "3000:3000"

  nginx:
    image: kilsiworld-nginx
    container_name: kilsiworld-nginx
    ports:
      - "80:80"
      - "4000:4000"
    depends_on:
      - kilsiworld-backend
    volumes:
      - certbot-etc:/etc/letsencrypt
      - certbot-www:/var/www/certbot

#  certbot:
#    image: certbot/certbot
#    container_name: certbot
#    volumes:
#      - certbot-etc:/etc/letsencrypt
#      - certbot-www:/var/www/certbot
#    entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew --force-renewal; sleep 12h & wait $${!}; done;'"

volumes:
  certbot-etc:
  certbot-www:
